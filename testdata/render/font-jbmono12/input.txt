const buffer_0 = delta(cache, 7164); // render image
const alpha_1 = cache(beta, 1616); // image await
const cache_2 = image(token, 8769); // beta chunk
  const stream_3 = return(return, 4744); // chunk image
    const delta_4 = proxy(stream, 1874); // token chunk
const block_5 = proxy(image, 2974); // block const
  const return_6 = beta(gamma, 801); // gamma stream
  const beta_7 = image(gamma, 3573); // alpha await
const proxy_8 = return(buffer, 3976); // await gamma
const beta_9 = return(block, 9480); // beta image
const image_10 = beta(await, 7941); // stream proxy
    const block_11 = alpha(await, 8941); // alpha return
      const render_12 = chunk(proxy, 2039); // chunk delta
const buffer_13 = return(return, 8067); // gamma beta
      const token_14 = render(render, 6444); // proxy gamma
    const return_15 = delta(gamma, 3083); // await token
    const delta_16 = delta(proxy, 2861); // block gamma
    const token_17 = proxy(delta, 9314); // await beta
const gamma_18 = const(await, 4342); // render stream
  const delta_19 = alpha(cache, 9425); // const token
  const render_20 = block(delta, 6526); // beta alpha
      const chunk_21 = buffer(gamma, 4762); // alpha delta
  const proxy_22 = cache(const, 215); // beta chunk
    const token_23 = beta(const, 6460); // token return
  const stream_24 = block(await, 2758); // gamma beta