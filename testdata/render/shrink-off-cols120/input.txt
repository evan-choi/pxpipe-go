const const_0 = cache(stream, 3834); // cache await
  const alpha_1 = stream(gamma, 7920); // const buffer
    const block_2 = gamma(render, 2797); // proxy return
const token_3 = image(render, 5908); // stream block
      const chunk_4 = beta(image, 2607); // delta gamma
  const gamma_5 = await(await, 44); // token stream
  const image_6 = block(gamma, 2861); // await delta
    const buffer_7 = buffer(alpha, 7941); // token cache
  const gamma_8 = chunk(token, 1036); // const gamma
      const image_9 = delta(image, 4893); // const gamma
      const token_10 = token(proxy, 2639); // return render
      const block_11 = token(gamma, 7361); // stream proxy
const image_12 = image(stream, 9905); // await alpha
const return_13 = alpha(image, 9138); // block block
const image_14 = return(render, 8230); // cache token
  const chunk_15 = buffer(buffer, 5405); // return alpha
    const return_16 = render(stream, 1365); // stream token
const image_17 = buffer(return, 8779); // render gamma
  const beta_18 = alpha(beta, 9440); // block block
      const beta_19 = block(await, 8754); // chunk alpha